## Summary

- **What problem does this PR solve?** `scroll_only` only supported vertical `swipeDirection: "up" | "down"` with no distance/path control. `scroll_and_resolve_ui_target` had divergent iOS vs Android implementations.
- **Why is this change needed now?** The flat `swipeDirection` enum ages poorly — a structured `gesture` model with optional viewport-relative ratios gives deterministic control needed for stable automation.
- **Why is this the right scope for one reviewable PR?** Two focused contract changes: (1) gesture model for `scroll_only`, (2) remove iOS `scroll_and_resolve_ui_target` to unify platforms. Both touch the same contract/adapter/test surfaces.

## Capability impact

- Capability category: action
- User-visible or AI-facing behavior change: `scroll_only` now requires `gesture: { direction, startRatio?, endRatio? }` instead of optional `swipeDirection`. `scroll_and_resolve_ui_target` is Android-only.
- Platforms/frameworks affected: Android / iOS
- Support boundary change: contract-ready — new `ScrollOnlyGesture` interface with typed direction and optional precision ratios
- Invocation guidance impact: canonical — `docs/guides/ai-agent-invocation.zh-CN.md` examples should reference new `gesture` shape
- Capability truth source checked: `packages/contracts/src/types.ts`
- Public docs / canonical guide update: no — follow-up PR to update invocation guides and READMEs

## Implementation notes

- Key implementation changes: Added `ScrollOnlyGesture` interface, `ScrollOnlyGestureMode` type, `buildScrollOnlySwipeCoordinates()` for 4-direction + ratio support, `normalizeScrollOnlyGesture()` validation.
- Contract / policy / session / evidence changes: `ScrollOnlyInput.gesture` required, `swipeDirection` removed from input and output. `ScrollOnlyData.gestureApplied` is single source of truth.
- Deterministic path and fallback behavior: no fallback — `gesture` is required, validation errors return `CONFIGURATION_ERROR`.
- AI-first capability guideline consulted: yes
- Compatibility, migration, or rollout notes: existing `swipeDirection` callers must migrate to `gesture: { direction }`. iOS `scroll_and_resolve_ui_target` callers must use `scroll_only → wait_for_ui → resolve_ui_target`.

## Validation

- [x] `pnpm build`
- [x] `pnpm typecheck`
- [ ] `pnpm test:ci`
- [x] `pnpm validate:architecture-guardrails` (CI-enforced)
- [ ] `pnpm validate:tool-output-contracts` (CI-enforced)
- [ ] Additional targeted validation noted below

### Validation evidence

- Targeted tests, flows, or fixtures: 462 adapter-maestro tests pass, 261 mcp-server tests pass, 31 core tests pass
- Device / simulator / environment notes: No live device testing in this PR — contract and unit-level validation only
- Screenshots, logs, or artifacts (if applicable): N/A

### Machine-checked vs reviewer-only

The following architecture guardrails are now **CI-enforced** (no longer reviewer-only):
- Hotspot file size limits (soft: 500 lines warn, hard: 1500 lines warn-first rollout)
- Thin-facade boundary violations in `adapter-maestro/src/index.ts`
- Platform leakage into pure model/config modules
- Dependency direction (no reverse imports from focused modules into orchestration)
- Tool catalog drift (registered tools vs README documentation)
- ToolResult envelope integrity and Tier 1 tool payload contracts

The following remain **reviewer-only** checks:
- AI-first capability guideline consultation quality
- Support boundary language appropriateness
- Deterministic-first design judgment
- Scope focus and reviewability

## Risks and rollback

- Main risks: Breaking change for existing `scroll_only` callers using `swipeDirection`. iOS `scroll_and_resolve_ui_target` callers will fail.
- Rollback strategy: Revert commits — old `swipeDirection` path and iOS branch remain in git history.
- Follow-up work (if any): Update `docs/guides/ai-agent-invocation.zh-CN.md`, READMEs, and `scroll_and_tap_element` for consistency.

## Checklist

- [x] No secrets, credentials, or local artifacts committed
- [ ] Docs updated when behavior or support boundary changed
- [ ] Invocation guidance updated or explicitly marked not needed when tool usage, sequencing, session rules, recovery routing, or support boundary changed
- [x] `docs/engineering/ai-first-capability-expansion-guideline.md` was reviewed for any capability-surface change (or explicitly marked not applicable)
- [x] Contracts/exports updated before downstream consumption when applicable
- [x] Policy / session / evidence impact reviewed when capability behavior changed
- [x] Deterministic-first behavior preserved, with explicit fallback semantics when used
- [x] `packages/adapter-maestro/src/index.ts` remains a thin facade only (exports + thin composition/coordinator), with no new runtime/platform/selector/policy logic added
- [x] No low-level helper backflow into `index.ts` and no reverse import that breaks intended module direction
- [ ] If touching `adapter-maestro` hotspots (`index.ts`/`ui-tools.ts`/`device-runtime.ts`/`recording-runtime.ts`), include line-count delta + boundary justification in PR notes
- [x] Scope is focused and reviewable

## Pre-push workflow (for guarded-path PRs)

1. Copy this template: `cp .github/PULL_REQUEST_TEMPLATE.md .pr-body.md`
2. Fill in all fields (replace placeholder values with actual content)
3. Validate locally: `pnpm validate:pr-gate .pr-body.md`
4. Push: the pre-push hook will re-validate `.pr-body.md` automatically
5. Create PR: `gh pr create --body-file .pr-body.md`
